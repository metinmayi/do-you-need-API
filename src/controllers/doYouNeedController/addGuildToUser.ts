import { Request, Response } from "express";

export async function addGuildToUser(req: Request, res: Response) {
  console.log("reached");
  // Check that we're authenticated
  // Validate request
  // Attempt to add the guild to the user
  // Return
}
